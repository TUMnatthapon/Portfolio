/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com','drive.google.com','www.fms.psu.ac.th']
  },
}

module.exports = nextConfig
